[package]
name = "plugin_impl"
version = "0.1.0"
authors = [
    "Simon Brummer <simon.brummer@posteo.de>"
]
edition = "2021"
license = "MPL-2.0"

[lib]
name = "plugin_impl"
crate-type = ["staticlib"]

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
